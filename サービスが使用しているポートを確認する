実行例：
サービス「A」がポート99999を使用しているか確認する。

対象サーバでコマンドプロンプトを起動する。

netstat -nao を実行する。

出力された中から以下のような行を探す。
TCP    0.0.0.0:99999            0.0.0.0:0              LISTENING       428

タスクマネージャを起動して、対象PID：428 が使用しているサービス名が A であることを確認する。

※PIDはその時々で変わる。
